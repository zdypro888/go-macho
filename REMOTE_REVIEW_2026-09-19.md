# 远程增量代码审查

范围：`4420f93d398738a90cf598709134a252b01d3931` → `0fb05614b9c51d1e3ac49a6215d455f738db3f51`，11 个提交、33 个变更文件。正确性判断依据为代码 diff 与调用语义，测试仅作辅助。

## 结论

**结论**：保留 reader 隔离、锁、分块读取、导出及解析修复；修正公开可变表的索引失效逻辑。

**证据**：
1. `Symtab.Syms` 可以在串行调用之间原地改写；切片身份和长度不变不代表名称/地址不变。只复核命中项无法发现新匹配或更早的重复项。名称索引重用前比较全部名称快照，变化时回退线性扫描。
2. 地址索引使用二分搜索，前提是当前值仍有序且同地址按原表顺序排列；原地修改破坏这两个前提时必须回退原表扫描。
3. `GetBindInfo` 暴露的 bind 表允许串行修改；完整比较 `Start+SegOffset` 快照才能保留首项匹配语义，名称从当前项读取。
4. `getOffset` 的 Memsz 判界承载 PAGEZERO 下的镜像相对偏移路径，本轮不改成 Filesz。Swift out-of-line 游标及 Type.Size 的既有调用顺序行为未另行改写。
5. 分块 LINKEDIT 导出保留段布局和当前格式约束；真实缓存中 libsystem_c 的已知导出拒绝也存在于冻结本地版本，不归因于此次远程变更。

**排除项**：
- ~~公开字段可变但仅检查索引命中就足够~~：会遗漏新名称、新地址和首项变化。
- ~~要求外部调用方接受新增不可变契约~~：不是原 API 的约束，不能用文档收窄掩盖回归。
- ~~验证可变字段仍能保持每次 O(1) 查找~~：没有写入代际标记时完整有效性检查是 O(n)，本次优先保持语义。

原始 default branch 为 master，保持不变。未声称所有未知格式或所有调用组合均无缺陷。
